{
  "entities": {
    "Todo": {
      "title": "Todo",
      "description": "A task that a user needs to complete.",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The description of the task."
        },
        "completed": {
          "type": "boolean",
          "description": "Whether the task is completed."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns the task."
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp of creation."
        }
      },
      "required": ["text", "completed", "userId", "createdAt"]
    },
    "User": {
      "title": "User",
      "description": "Registered user profile.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "User email address."
        },
        "displayName": {
          "type": "string",
          "description": "User display name."
        },
        "photoURL": {
          "type": "string",
          "description": "User profile photo URL."
        },
        "lastLoginAt": {
          "type": "number",
          "description": "Timestamp of last login."
        },
        "clockState": {
          "type": "string",
          "description": "clock in or out state"
        },
        "clockTime": {
          "type": "string",
          "description": "time of clock state"
        }
      },
      "required": ["email", "lastLoginAt"]
    },
    "Event": {
      "title": "Event",
      "description": "Calendar event",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "title": { "type": "string" },
        "date": { "type": "string" },
        "type": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["userId", "title", "date", "type", "createdAt"]
    },
    "Customer": {
      "title": "Customer",
      "description": "CRM Customer",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "phone": { "type": "string" },
        "company": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["userId", "name", "email", "phone", "company", "status", "createdAt"]
    }
  },
  "firestore": {
    "todos/{todoId}": {
      "schema": {
        "$ref": "#/entities/Todo"
      },
      "description": "Stores user tasks."
    },
    "users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores registered user profiles."
    },
    "events/{eventId}": {
      "schema": {
        "$ref": "#/entities/Event"
      },
      "description": "Stores calendar events"
    },
    "customers/{customerId}": {
      "schema": {
        "$ref": "#/entities/Customer"
      },
      "description": "Stores CRM customers"
    }
  }
}
